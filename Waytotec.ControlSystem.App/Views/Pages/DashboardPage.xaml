<Page
    x:Class="Waytotec.ControlSystem.App.Views.Pages.DashboardPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:Waytotec.ControlSystem.App.Views.Controls"
    xmlns:converters="clr-namespace:Waytotec.ControlSystem.App.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Waytotec.ControlSystem.App.Views.Pages"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Title="DashboardPage"
    d:DataContext="{d:DesignInstance local:DashboardPage,
                                     IsDesignTimeCreatable=False}"
    d:DesignHeight="450"
    d:DesignWidth="800"
    ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
    ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    mc:Ignorable="d">

    <Page.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
        <converters:BoolToStringConverter x:Key="BoolToStringConverter" />
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="200" />
            <RowDefinition Height="3*" />
            <RowDefinition Height="2*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*" />
            <ColumnDefinition Width="2*" />
        </Grid.ColumnDefinitions>

        <!--  눈내리는 효과  -->
        <Grid
            Grid.Row="0"
            Grid.ColumnSpan="2"
            Height="200"
            ClipToBounds="True">
            <Grid>
                <Rectangle
                    Height="200"
                    VerticalAlignment="Bottom"
                    Panel.ZIndex="0"
                    RadiusX="8"
                    RadiusY="8">
                    <Rectangle.Fill>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                            <GradientStop Offset="0" Color="{DynamicResource SnowflakeGradientTop}" />
                            <GradientStop Offset="1" Color="{DynamicResource SnowflakeGradientBottom}" />
                        </LinearGradientBrush>
                    </Rectangle.Fill>
                </Rectangle>

                <Grid Margin="10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" Panel.ZIndex="1">
                        <ui:SymbolIcon
                            x:Name="MainSymbolIcon"
                            FontSize="100"
                            Symbol="Symbols20" />
                        <TextBlock
                            x:Name="MainTitle"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            FontSize="28"
                            FontWeight="Black"
                            Text="MainPage" />
                    </StackPanel>

                    <TextBlock
                        Grid.Column="1"
                        Margin="20"
                        HorizontalAlignment="Center"
                        Panel.ZIndex="-1"
                        FontSize="12"
                        Foreground="{DynamicResource TextControlPlaceholderForeground}"
                        TextWrapping="Wrap">
                        WPF UI provides the Fluent experience in your known and loved WPF framework. Intuitive design, themes, navigation and new immersive controls. All natively and effortlessly. Library changes the base elements like Page, ToggleButton or List, and also includes additional controls like Navigation, NumberBox, Dialog or Snackbar.<LineBreak />
                        <LineBreak />
                        Support the development of WPF UI and other innovative projects by becoming a sponsor on GitHub! Your monthly or one-time contributions help us continue to deliver high-quality, open-source solutions that empower developers worldwide.</TextBlock>
                </Grid>

                <Canvas x:Name="MainCanvas" Panel.ZIndex="2" />
            </Grid>
        </Grid>

    </Grid>
</Page>