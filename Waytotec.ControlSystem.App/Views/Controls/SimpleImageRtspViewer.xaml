<UserControl
    x:Class="Waytotec.ControlSystem.App.Views.Controls.SimpleImageRtspViewer"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Waytotec.ControlSystem.App.Views.Controls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <Grid>
        <!--  비디오 이미지 표시 영역  -->
        <Image
            x:Name="VideoImage"
            Stretch="Uniform"
            StretchDirection="Both" />

        <!--  로딩 오버레이  -->
        <Border
            x:Name="LoadingOverlay"
            Background="#80000000"
            Visibility="Collapsed">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <ProgressBar
                    Width="100"
                    Height="4"
                    Foreground="#4CAF50"
                    IsIndeterminate="True" />
                <TextBlock
                    x:Name="LoadingText"
                    Margin="0,10,0,0"
                    HorizontalAlignment="Center"
                    Foreground="White"
                    Text="연결 중..." />
            </StackPanel>
        </Border>

        <!--  상태 정보 오버레이  -->
        <StackPanel
            Margin="10"
            HorizontalAlignment="Right"
            VerticalAlignment="Bottom"
            Background="#80000000">
            <TextBlock
                x:Name="OverlayFps"
                Margin="5,2"
                FontSize="11"
                Foreground="White"
                Text="FPS: 0" />
            <TextBlock
                x:Name="OverlayResolution"
                Margin="5,2"
                FontSize="11"
                Foreground="White"
                Text="해상도: -" />
            <TextBlock
                x:Name="OverlayTime"
                Margin="5,2"
                FontSize="11"
                Foreground="White"
                Text="경과시간: 0초" />
        </StackPanel>
    </Grid>
</UserControl>
