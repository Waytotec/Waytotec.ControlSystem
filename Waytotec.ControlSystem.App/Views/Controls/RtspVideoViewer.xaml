<UserControl x:Class="Waytotec.ControlSystem.App.Views.Controls.RtspVideoViewer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Waytotec.ControlSystem.App.Views.Controls"
             Width="Auto"
             Height="Auto"
             mc:Ignorable="d">
    <Grid>
        <Image x:Name="VideoImage"
               Stretch="Uniform" />
        <StackPanel Margin="10"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Top">
            <TextBlock x:Name="OverlayFps"
                       Foreground="White"
                       FontWeight="Bold"
                       FontSize="14" />
            <TextBlock x:Name="OverlayResolution"
                       Foreground="White"
                       FontWeight="Bold"
                       FontSize="14" />
            <TextBlock x:Name="OverlayTime"
                       Foreground="White"
                       FontWeight="Bold"
                       FontSize="14" />
        </StackPanel>
        <!-- 로딩 인디케이터 -->
        <Border x:Name="LoadingOverlay"
            Background="#80000000"
            Visibility="Collapsed"
            CornerRadius="10"
            Width="100" Height="100"
            HorizontalAlignment="Center"
            VerticalAlignment="Center">
            <TextBlock Text="로딩중..." Foreground="White" 
                   HorizontalAlignment="Center" 
                   VerticalAlignment="Center"
                   FontWeight="Bold"/>
        </Border>
    </Grid>
</UserControl>
